# tutorial_pandas_2017
Tutorial de pandas para taller CinvesPy

Se puede crear un entorno de conda con los paquetes necesarios usando el siguiente comando:
```
$ conda env create -f environment.yml
```